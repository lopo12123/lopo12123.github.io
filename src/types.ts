export type EssayMeta = {
    id: string
    title: string
    poster?: string
    datetime: string
    tag: string[]
}

export type ProjectMeta = {
    id: string
    name: string
    description: string
    logo?: string
}